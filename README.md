# AI Driving Instructor

The EA Sports WRC telemetry MVP lives in [`beamng/TripCompiler`](beamng/TripCompiler).

It records the simulator's official UDP telemetry to an immutable JSONL capture and compiles
the capture into normalized telemetry, detected driving events, a JSON summary, and an HTML
report.

