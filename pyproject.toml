[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-driver-tripcompiler"
version = "0.1.0"
description = "Compile OBD and EA Sports WRC data into a common driving analysis."
readme = "README.md"
requires-python = ">=3.10,<3.14"
license = { text = "MIT" }
authors = [{ name = "AI Driving Instructor" }]
dependencies = []

[project.optional-dependencies]
dev = [
  "mypy>=1.11,<2",
  "pytest>=8.3,<9",
  "pytest-cov>=5,<7",
  "ruff>=0.6,<1",
  "pre-commit>=3.8,<5",
]

[project.scripts]
tripcompiler = "tripcompiler.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
tripcompiler = ["config/*.json"]

[tool.ruff]
target-version = "py310"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "RUF"]
ignore = ["E501"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"

[tool.mypy]
python_version = "3.10"
strict = true
warn_unused_configs = true
packages = ["tripcompiler"]
mypy_path = "src"

[tool.pytest.ini_options]
addopts = "-ra --strict-markers --strict-config --basetemp=.codex-pytest-tmp --cov=tripcompiler --cov-report=term-missing --cov-fail-under=75"
testpaths = ["tests"]
pythonpath = ["src"]

[tool.coverage.run]
source = ["tripcompiler"]
branch = true

[tool.coverage.report]
show_missing = true
skip_covered = true
fail_under = 75
